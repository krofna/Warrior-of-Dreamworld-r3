#########################
## MySql Database info ##
#########################
@Databases
# WorldDatabase #
#################
# protocol://address:port (default: tcp://127.0.0.1:3306)
tcp://127.0.0.1:3306
# Username (default: root)
root
# Password (default: root)
root
# Schema (default: wod_world)
world
#
######################
# CharactersDatabase #
######################
# protocol://address:port (default: tcp://127.0.0.1:3306)
tcp://127.0.0.1:3306
# Username (default: root)
root
# Password (default: root)
root
# Schema (default: wod_world)
# CharactersDatabase schema (default: wod_characters)
wod_characters
